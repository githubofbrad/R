🔷R 변수 넣기🔷

1. 기본적인 변수 넣기
  ex) a <- 1
   → a라는 변수에 1을 넣어줌. 화살표 사용의 단축키는 [ctrl]+[-]

2. 벡터
  a <- c(1, 2, 3) 
   *[]연산자를 통해 접근할 수 있음 
    ex) a[2] = 2
   **콜론(:)을 통해 간편하게 접근 가능
    ex) a <- c(1:20)
    → 1에서부터 20까지 전부 적는 것이 아닌 콜론(:)을 사용하여 나타낼 수 있음
